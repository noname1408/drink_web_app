@model HomeViewModel

@await Html.PartialAsync("Carousel")
<h2>Các loại đồ uống được yêu thích nhất trong tuần này</h2>
<h4>Có thể bạn cũng thích !</h4>


<div class="row">
    @foreach (var drink in Model.PreferredDrinks)
    {
        @Html.Partial("DrinkSummary",drink)
    }
</div>