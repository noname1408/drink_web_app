<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Đồ uống <span class="caret"></span></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Drinks" asp-action="List" asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>

        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Drinks" asp-action="List" asp-route-category="">Tất cả</a>
        </li>
    </ul>
</li>