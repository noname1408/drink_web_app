
@model Drink


<div class="col-sm-4 col-lg-4 col-sm-4">
    <div class="thumbnail ">
        <img  class="img-responsive" src="@Model.ImageThumbnailUrl" alt="@Model.Name" />
        <div class="caption">
            <h3 class="pull-right">@Model.Price.ToString("c")</h3>
            <h3>@Model.Name</h3>
            <p> @Model.ShortDescription</p>
        </div>
    </div>
</div>